#include<stdio.h>
int main()
{
	int a,b,c,d,max;
	printf("请输入三个数字a,b,c",a,b,c);
	scanf("%d%d%d",&a,&b,&c);
	if(a>b)
		c=a;
	else&&(if(d>c))
			max=d;
		else
			max=c; 
	printf("最大数max=%d",max);
	return 0;
}
